/*export default function AnimationText({ text }) {
    return (
        <>
            {text.split("").map((letter, index) => {
                <span
                    key={index}
                    style={{ animationDelay: `${index * 0.1}s` }}
                    className="inline-block animate-text"
                >
                    {letter}
                </span>;
            })}
        </>
    );
}*/
